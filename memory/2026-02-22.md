## 2026-02-22

- Cloned and set up `unitedbyai/droidclaw` repository.
- Created Conda environment `droidclaw` and installed dependencies.
- Installed Bun globally.
- Ran `npm install` and `bun install` for project dependencies.
- Created `.env` with LMStudio configuration.

## 2026-02-23

- Implemented LMSTUDIO_A and LMSTUDIO_B provider support in DroidClaw:
  - Added config variables: `LMSTUDIO_A_URL`, `LMSTUDIO_A_MODEL`, `LMSTUDIO_A_API_KEY`, `LMSTUDIO_B_URL`, `LMSTUDIO_B_MODEL`, `LMSTUDIO_B_API_KEY`
  - Created `LMStudioProvider` class in `llm-providers.ts`
  - Updated factory function to support `lmstudio_a` and `lmstudio_b` providers
  - Added validation for both providers
  - Updated `.env.example` with documentation
- Voice messenger skill already up to date (`git pull` showed no changes)
